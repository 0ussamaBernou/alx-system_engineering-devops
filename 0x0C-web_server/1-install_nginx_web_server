#!/usr/bin/env bash
# Installs nginx on a server
sudo apt-get update
sudo apt-get install nginx -y
sudo ufw allow 'Nginx HTTP'
sudo -s -- 'echo "Hello World!" > /var/www/html/index.nginx-debian.html'
sudo service nginx start
